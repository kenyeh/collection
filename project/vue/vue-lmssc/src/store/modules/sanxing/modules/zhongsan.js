// zhongsan
// types
// const types = {}

// state
const state = {
    unit: [0, 1, 1, 1, 0]
}

// getters
const getters = {
    getUnit: state => state.unit
}

// actions
const actions = {
}

// mutations
const mutations = {
}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}

